CREATE TABLE characters  (
  id INTEGER PRIMARY KEY,
  name TEXT, 
  motto TEXT, 
  species TEXT,
  author_id INTEGER, 
  series_id INTEGER
);

CREATE TABLE books (
  id INTEGER PRIMARY KEY,
  
);
CREATE TABLE series (
  id INTEGER PRIMARY KEY,
  
);
CREATE TABLE authors (
  id INTEGER PRIMARY KEY,
  
);
CREATE TABLE sub_genre (
  id INTEGER PRIMARY KEY,
  
);